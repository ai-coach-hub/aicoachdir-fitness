'use client';import type{PreviewExercise}from '@/lib/fitness/previewData';import s from './FitnessPreview.module.css';export default function ExerciseVideoDemo({exercise}:{exercise:PreviewExercise}){if(exercise.videoState==='published'&&exercise.videoUrl)return <div className={s.videoFrame}><video className={s.video} src={exercise.videoUrl} controls muted loop playsInline preload="metadata"/></div>;return <div className={s.videoFrame}><div className={s.fallback}><strong>Professional demo queued</strong><div>The exercise is already connected to its video slot. The approved human demo will appear here automatically when published.</div><ul className={s.cueList}>{exercise.cues.slice(0,3).map(c=><li key={c}>{c}</li>)}</ul></div></div>}